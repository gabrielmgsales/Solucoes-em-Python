preco_dia = 60
preco_km = 0.15

dias = int(input('Quantos dias: '))
km = float(input('Quantos Km: '))

preco_total = (preco_dia * dias) + (km * preco_km)

print(f'O carro foi alugado por {dias} e rodou {km}')

print(f'Total a pagar: R${preco_total:.2f}')
